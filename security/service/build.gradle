serviceWithContract('security', ['v1'])

dependencies{
    compile("org.springframework.cloud:spring-cloud-starter-oauth2")
    compile project(':user-client-v1')
    compile project(':spring-security')
}